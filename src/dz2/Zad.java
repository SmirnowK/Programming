package dz2;

public class Zad {
    public int sum(int q, int w, int e, int r, int t, int y, int u, int i, int o, int p) {
        int result = (q + w + e + r + t + y + u + i + o + p);
        return result;
    }
}
